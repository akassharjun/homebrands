import 'package:flutter/material.dart';

class OrderPage extends StatelessWidget {
	final String name;
	final int quantity;

  const OrderPage({Key key, this.name, this.quantity}) : super(key: key);
	
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return null;
  }
	
}